#:kivy 1.11.1

<NoteScreen>:
    Button:
        text: 'Notes'

<TempoScreen>:
    Button:
        text: 'Tempo'


<ToggleButton@Image>:
    source: None
    background_normal: ''

    Image:
        source: root.source
        pos: root.pos
        size: root.size


#### ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~Instrument screen stuff below~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ####



<TriangleButtonShapes@Widget>: #class to create 2 triangle shape graphics (to appear as the buttons for switching screens - no actual functionality, just graphics.)

    FloatLayout:
        size: root.size
        triangle_mul: .5
        canvas:
            Color:
                rgba: 1, 0, 0, 1
            Triangle:
                points: self.x + self.width * 0.01, self.y + self.height * self.triangle_mul, self.x + self.width * .06, self.y + self.height * self.triangle_mul + self.height * .05, self.x + self.width * .06, self.y + self.height * self.triangle_mul - self.height * .05
            Triangle:
                points: self.x + self.width * 0.99, self.y + self.height * self.triangle_mul, self.x + self.width * 0.94, self.y + self.height * self.triangle_mul + self.height * .05, self.x + self.width * 0.94, self.y + self.height * self.triangle_mul - self.height * .05

<LeftInvisibleTransitionButton@Button>: #class that creates a transparent button located at the left triangle - press to switch screens.
    size_hint: .1, .2
    pos_hint: {'y': .4}
    background_color: 0, 0, 0, 0

<RightInvisibleTransitionButton@Button>: #class that creates a transparent button located at the right triangle - press to switch screens.
    size_hint: .1, .2
    pos_hint: {'x': .9, 'y': .4}
    background_color: 0, 0, 0, 0

<InstrumentScreen>:
    FloatLayout:

        LeftInvisibleTransitionButton:
            on_press: root.manager.current = "tempo"

        RightInvisibleTransitionButton:
            on_press: root.manager.current = "notes"

        TriangleButtonShapes:

        GridLayout:
            size_hint_x: .8
            pos_hint: {'x':.1}
            cols: 3
            padding: 10, 10, 10, 10
            spacing: 10, 10
            ToggleButton:
                group: 'btn'
                source: 'Images\AcousticGuitar.jpg'
            ToggleButton:
                group: 'btn'
                source: 'Images\BrassSection.jpg'
            ToggleButton:
                group: 'btn'
                source: 'Images\ElectricBass.jpg'
            ToggleButton:
                group: 'btn'
                source: 'Images\ElectricPiano.jpg'
            ToggleButton:
                group: 'btn'
                source: 'Images\GrandPiano.jpg'
            ToggleButton:
                group: 'btn'
                source: 'Images\RockOrgan.jpg'
            ToggleButton:
                group: 'btn'
                source: 'Images\StringEnsemble.jpg'
            ToggleButton:
                group: 'btn'
                source: 'Images\Synth1.jpg'
            ToggleButton:
                group: 'btn'
                source: 'Images\Synth2.jpg'










